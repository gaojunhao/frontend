<!--miniprogram/pages/home/home.wxml-->
<!--
<view>
			<view class="weui-elect-bar">
			<view class="elect-bar-inner">
				<checkbox-group class="elect-bar-inner" bindchange="OnCheckChange">
					<checkbox>显示已租出</checkbox>
				</checkbox-group>
			</view>
			<view class="elect-bar-inner">
				<view class="font3">租金预算：</view>
				<input type="number" class="inputRent" maxlength="5" bindinput="OnDownInput"></input>
				<view class="font2">~</view>
				<input type="number" class="inputRent" maxlength="5" bindinput="OnUpInput"></input>
			</view>
		</view>
	</view>
	-->
	<!--
<view class="weui-search-bar">
<view class="weui-search-bar__form">

	<view class="weui-search-bar__box">
		<icon class="weui-icon-search_in-box" type="search" size="14"></icon>
		<input type="text" class="weui-search-bar__input" placeholder="请输入房屋名称或房型关键词" bindinput="OnSearchInput" />
	</view>
</view>

<view class="weui-search-bar__cancel-btn" bindtap='OnSearchClick'>搜索</view>
</view>
-->
<!--
<view class="weui-search-bar-inside"></view>
<view class="weui-search-bar">
	<view class="weui-address-bar__cancel-btn" bindtap='OnAddressClick'>位置</view>
	<icon class="weui-icon-address_in-box" type="address" size="14"></icon>

<view class="weui-search-bar__cancel-btn" bindtap='OnSearchClick'>搜索</view>
<icon class="weui-icon-search_in-box" type="search" size="14"></icon>
</view>
-->
<!--
<view class="weui-search-bar-inside">
		
<view class="weui-search-bar__form">
搜索框
	<view class="weui-search-bar__box">
		<icon class="weui-icon-search_in-box" type="search" size="14"></icon>
		<input type="text" class="weui-search-bar__input" placeholder="请输入房屋名称或房型关键词" bindinput="OnSearchInput" />
	</view>
</view>

<view class="weui-search-bar__cancel-btn" bindtap='OnSearchClick'>搜索</view>
</view>-->
<!-- 下拉菜单 -->
<view class="menu-table">
<block wx:for='{{houses}}' wx:key="item">
		<view class="container" data-id="{{item.id}}" bindtap="onItemClick">
			<view class='container-img'>
				<image mode="aspectFill" class="img-prev" src="{{item.img[0]}}" lazy-load="true"></image>
			</view>

			<view class='container-content'>
				<view class='house-title'>
					<view class='house-ads'>
						<view class='user-name'>{{item.ads}}</view>
					</view>
				</view>

				<view class='content'>

					<text class='content-maxg'>所在楼层：{{item.maxg}}</text>

					<view class="tagView">
						<text class="tagText-n">{{item.ditie}}</text>
						<view wx:for="{{item.type}}" wx:key="tag" wx:for-item="tag">
							<text class="tagText">{{tag}}</text>
						</view>
					</view>

					<view class='font-rent'>{{item.rent}} <text class="font-rent-unit">元/月</text></view>
				</view>
		</view>
	</view>
		<view class='container-line'></view>
</block>
</view>
<button class="add-button" type="primary" bindtap="OnPostHouseClick" wx:if="{{ident=='host'}}">+</button>
<view class='menu-po'>
        <!-- 导航栏 -->
        <view class='menu'>
            <view class='menu-list'>
                <view class='menu-nav' wx:for="{{ msgList }}" wx:for-index="idx" wx:key="key" data-hi="{{ idx }}" bindtap="menuClick">
                    <text class='menu-text'>{{ item.name }}</text>
                    <image class='menu-image {{!meunShow[idx].isShows&&"select_img_rotate"}}' src='/images/san1.png'></image>
                </view>
            </view>
        </view>
        <!-- 列表栏 -->
        <view class='menu-if'>
            <view class="meun-txt" hidden='{{ meunShow[0].isShows ? true : false }}'>
                <view class='menu-text-list'>
                    <view class='menu-text-list-left over-auto'>
                        <text wx:for="{{ areaLise }}" wx:key="name" class='meun-city-txt' bindtap="rowClick"  data-hi="{{ index }}">{{ item.name }}</text>
                    </view>
                    <view class='menu-text-list-rig over-auto' hidden='{{ rigShow ? true : fasle }}'>
                        <view class='menu-city' hidden='{{ rowShow[0].isShows ? true : fasle }}'>
                            <text wx:for="{{ rowLise.row_ditie.name }}" wx:key="item" class='meun-city-txt' bindtap="ditieClick" data-hi="{{ index }}">{{ item }}</text>
                        </view>
                        <view class='menu-city' hidden='{{ rowShow[1].isShows ? true : fasle }}'>
                            <text wx:for="{{ rowLise.row_quyu.name }}" wx:key="item" class='meun-city-txt' bindtap="quyuClick" data-hi="{{ index }}">{{ item }}</text>
                        </view>
                    </view>
                </view> 
            </view>
            <view class="meun-txt over-auto" hidden='{{ meunShow[1].isShows ? true : false }}'>
                <text wx:for="{{ price }}" wx:key="id" class='meun-city-txt meun-city-txt-price' bindtap="priceClick" data-hi="{{ index }}">{{ item.name }}</text>
            </view>
            <view class="meun-txt over-auto" hidden='{{ meunShow[2].isShows ? true : false }}'>
                <text class='meun-city-txt meun-city-txt-price' bindtap="roomModelClick" data-hi="{{ index }}" wx:for="{{ roomModel }}" wx:key="id">{{ item.name }}</text>
            </view>
            <view class="meun-txt" hidden='{{ meunShow[3].isShows ? true : false }}'>
							<text class='meun-city-txt meun-city-txt-price' bindtap="rentTypeClick" data-hi="{{ index }}" wx:for="{{ rentType }}" wx:key="id">{{ item.name }}</text>
            </view>
        </view>
		</view>
		

